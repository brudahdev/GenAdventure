/** Run-scoped DI token for the resolved player name (from `SimStartData.userName`).
 *  Registered in `SimProvisioner` and consumed by `playerIdentityFactory`. */
export const USER_NAME = 'UserName'
