export interface NearbyLocationSummary {
    subLocations: {
        id: string,
        // todo displayName: string
    }[]

    locations: {
        id: string,
        // todo displayName: string
    }[]
}
